{
  "name": "timeful-e2e",
  "private": true,
  "description": "Playwright checks run against a deployed instance, by hand after a deploy.",
  "scripts": {
    "login": "node prod_login.js",
    "smoke": "node smoke_prod.js"
  },
  "dependencies": {
    "playwright": "^1.61.1"
  }
}
