# Optional. Google Maps Platform *browser* key, used only for address
# suggestions in the event location field. Leave unset and the location
# inputs stay plain free-text — nothing else changes.
# Enable "Places API (New)" + "Maps JavaScript API" on the key, restrict it by
# HTTP referrer, and keep it restricted to those two APIs.
VUE_APP_GOOGLE_MAPS_API_KEY=?
